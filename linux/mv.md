 1. Replace content of non-empty dir with another content from another dir:
    
    
    ```
    mv --backup=simple src dest
    ```
    
    Which will rename the `dest` to `dest~` and move the `src` to `dest`.
